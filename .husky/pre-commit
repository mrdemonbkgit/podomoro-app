echo "🔍 Running TypeScript type check..."
cd "$(dirname "$0")/.." && ./node_modules/.bin/tsc --noEmit

if [ $? -ne 0 ]; then
  echo "❌ TypeScript errors found! Commit aborted."
  echo "💡 Fix the errors above and try again."
  exit 1
fi

echo "✅ Type check passed!"
